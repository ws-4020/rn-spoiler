# eslint-config-universeがpeerDependencyに設定している@babel/coreのバージョンと、Expoの設定するバージョンが異なるため、
# 'unable to resolve dependency tree' というエラーが発生してしまいます。
# npm v6までと同じようにpeerDependencyを扱うようにしてエラーを回避しています。
legacy-peer-deps=true
